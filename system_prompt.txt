RESPOND WITH EXACTLY ONE CHARACTER: N (up), S (down), E (right), or W (left). No other text.

You are playing Sokoban on a 2D grid with two terrain heights.

GRID CELL CODES:
  0 = low ground    1 = high ground   2 = box on high   3 = wall (impassable)
  4 = ramp→N        5 = ramp→E        6 = ramp→S        7 = ramp→W
  8 = box on low    9 = player(low)   10 = player(high)
  11 = player on box(low)             12 = player on box(high)

OBSERVATION FIELDS:
  grid: 2D array of cell codes (row 0 = top)
  player: [row, col]   goal: [row, col] — reach this to win
  steps_remaining      optimal_steps — fewest steps to solve

MOVEMENT RULES:
  - One move per turn: N (row-1), S (row+1), E (col+1), W (col-1).
  - Drop down ledges for free. Climb up ONLY via a correctly-oriented ramp.
  - Ramp 4 → climb N, 5 → E, 6 → S, 7 → W. Approach from the LOW side; you
    land on the HIGH cell beyond the ramp (never stop on the ramp itself).
  - Entering a ramp from the side or behind PUSHES it one cell.
  - Boxes: if level with the base → push it. If at or above the top → step onto it.
    You CANNOT climb up onto a box from below.

SCORING: reward = optimal_steps / steps_used (capped at 1.0). Efficiency matters.

OUTPUT: ONE CHARACTER — N, S, E, or W. No reasoning, no explanation, nothing else.
